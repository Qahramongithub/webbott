#version: '3.8'
#
#services:
#  web:
#    build:
#      context: .
#      dockerfile: Dockerfile
#    command: gunicorn --workers 3 root.wsgi:application
#    volumes:
#      - .:/app
#      - static_volume:/app/static
#      - media_volume:/app/media
#    expose:
#      - "8000"
#    environment:
#      - DEBUG=False
#    ports:
#      - "8000:8000"
#    depends_on:
#      - webbot
#
#  webbot:
#    image: nginx:latest
#    volumes:
#      - ./webbot.conf:/etc/nginx/conf.d/default.conf
#      - static_volume:/static
#      - media_volume:/media
#    ports:
#      - "80:80"
#
#
#volumes:
#  static_volume:
#  media_volume:
version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    command: gunicorn root.wsgi:application --bind 127.0.0.1:8000
    volumes:
      - .:/app
      - static_volume:/app/static
      - media_volume:/app/media
    expose:
      - "8000"
    environment:
      - DEBUG=False
    ports:
      - "8000:8000"
    depends_on:
      - webbot

  webbot:
    image: nginx:latest
    volumes:
      - ./webbot.conf:/etc/nginx/conf.d/default.conf
      - static_volume:/static
      - media_volume:/media
    ports:
      - "80:80"

volumes:
  static_volume:
  media_volume:

